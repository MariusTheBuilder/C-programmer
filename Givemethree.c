#include <stdio.h>
// De 3 tal der angives, når programmet køres, lægger dem sammen og dividerer det med 3.
int main(void)

{float
first_number, xxx, sidsteTal;

    printf("Give me three: ")

;scanf("%f %f %f", & first_number, & xxx, & sidsteTal);  

    printf("The result: %f\n", (first_number + xxx + sidsteTal) / 3.0);  
  
return 0;
}